version: "3"
services:
  frontend:
    container_name: frontend
    build:
      context: ./Front
      dockerfile: Dockerfile
    # volumes:
    #   - ./nginx/dist/:/app/dist
    ports:
      - "5173:5173"
  server:
    container_name: server
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "9999:9999"

  database:
    container_name: database
    image: mysql:latest
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=neighbrewbe-10
      - MYSQL_DATABASE=neighbrew
      - MYSQL_PASSWORD=neighbrew-10
      # - MYSQL_USER=neighbrewbe
